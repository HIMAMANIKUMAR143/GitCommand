PORT=5000
NODE_ENV=development
# If Postgres is running (e.g. via docker-compose up -d or local server):
DATABASE_URL=postgresql://postgres:postgrespassword@localhost:5432/git_reset_lab
# If DATABASE_URL is not reachable, the app automatically falls back to an embedded PostgreSQL in-memory database!
